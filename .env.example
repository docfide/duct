# Duct — Environment Configuration
# At least one embedding provider is required for vector search.
# Without any, Duct falls back to keyword (BM25) search.

# OpenAI (recommended)
OPENAI_API_KEY=sk-...

# Google Gemini (alternative)
GEMINI_API_KEY=...
